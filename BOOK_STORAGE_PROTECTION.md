# حماية كتب المكتبة

تم تجهيز طبقة حماية لكتب SQLite التي يتم تنزيلها للمكتبة.

- الكتب تُخزن داخل `context.filesDir`، أي التخزين الداخلي الخاص بالتطبيق.
- الملف المثبت لا يحتفظ باسم `book_N.db` ولا بامتداد SQLite؛ يتم تخزينه باسم SHA-256 مبهم داخل `library/books/.vault/` وبامتداد `.qsb`.
- المحتوى يُشفّر باستخدام AES-256-GCM. مفتاح التشفير يُنشأ ويُحفظ داخل Android Keystore ولا يوضع داخل الكود أو ملف الكتالوج.
- أثناء التنزيل يتم التحقق من الحجم وSHA-256 وقابلية قراءة SQLite قبل التشفير.
- النسخة غير المشفرة تُستخدم فقط كملف مؤقت أثناء التثبيت، ثم تُحذف.
- عند القراءة، تُفك نسخة مؤقتة داخل `cacheDir` فقط لفتح SQLite، ثم يُحذف الملف المؤقت مباشرة بعد فتح قاعدة البيانات.
- عند اكتشاف تثبيت قديم يحتوي على `book_N.db` غير مشفر، يحاول مدير المكتبة تشفيره وحذف النسخة النصية.
- حذف الكتاب يحذف النسخة المحمية، مع إزالة بقايا النسخ القديمة إن وجدت.

هذه الحماية تمنع الوصول العادي إلى ملفات الكتب ونسخ SQLite الخام، لكنها ليست حماية مطلقة ضد جهاز مروّت أو تحليل عميق لتطبيق يعمل ويحتاج إلى فك المحتوى أثناء القراءة.

- `android:allowBackup` is disabled in the application manifest to reduce exposure through Android backup/export mechanisms.
- Release downloads point to the fixed `books-v1` GitHub Release and are verified against the catalog hash before encryption.
